# What MOABB did, and what MOECoG copies

MOABB (Mother of all BCI Benchmarks, NeuroTechX) is the model for this project. Facts below were read from the
repository on 2026-09-10 (fork at `yifan-study/moabb`, upstream `NeuroTechX/moabb`) and from GitHub.

## The facts

| question | MOABB | MOECoG today |
|---|---|---|
| open source? | yes, BSD-3-Clause, on GitHub since 2017-05 | yes, BSD-3-Clause, GitHub since 2026-09 |
| scale | 1,053 stars, 263 forks, 84 contributors, 17 GitHub releases (v1.7.1 on 2026-08-31), 124 open issues | one contributor, no release yet |
| package | `pip install moabb` (PyPI), dynamic version from `moabb.__version__`, optional extras `deeplearning`, `optuna`, `carbonemission`, `tests`, `docs` | `pip install -e .` only; extras `deep`, `nwb`, `loaders`, `viz`, `anatomy`, `dev` |
| hosting | docs at moabb.neurotechx.com (Sphinx, pydata theme, sphinx-gallery examples) deployed by GitHub Actions to GitHub Pages; Zenodo DOI per release (10.5281/zenodo.10034223) | docs are Markdown in the repo; no site, no DOI |
| results hosting | `results/*.csv` and `.json` committed in the repo (52 KB), rendered as DataTables on the "paper results" page; a benchmark paper anchors them (Chevallier et al. 2024, HAL hal-04537061) | `results/*.csv` + `docs/leaderboard.md`; smoke table; dataset atlas |
| how people contribute | fork, branch from `develop`, PR, pre-commit (ruff, black, isort, prettier), `whats_new.rst` entry with PR number and author link, review by maintainers; dataset tutorial in docs; `LocalBIDSDataset` lets anyone use their own BIDS data without a class | this guide (`CONTRIBUTING.md`), issue and PR templates, CI |
| pipelines | YAML files in `pipelines/` (`name`, `paradigms`, `citations`, list of `from`/`name`/`parameters` steps), parsed by `parse_pipelines_from_directory`; parameter contexts in `contexts/*.yml` | YAML in `pipelines/`, loaded by `moecog.pipelines.load_pipelines` |
| one-call benchmark | `moabb.benchmark(pipelines=, paradigms=, evaluations=, include_datasets=, ...)` runs everything and writes results and plots | `moecog.benchmark(...)` (same idea, within-subject evaluation) |
| dataset summary | auto-generated table (subjects, channels, classes, trials, trial length, rate, sessions, runs) from class attributes, plus a PapersWithCode link per dataset | `docs/decodable_datasets.md` from the catalog and smoke records |
| CI | GitHub Actions: tests on Python 3.10-3.14 (ubuntu, macOS, windows) with an MNE-data cache, docs build, link check, what's-new check, download test; pre-commit.ci | `ci.yml`: ruff + fast synthetic tests on 3.10-3.12 (ubuntu) and 3.12 (macOS) |
| governance | maintainers named in `pyproject.toml`, NeuroTechX community, code of conduct, `CITATION.cff` with ORCIDs, two papers (JNE 2018 framework paper; 2024 benchmark paper) | maintainer: Yifan Yu; code of conduct; `CITATION.cff`; papers to come |

## What we copy

1. **Package first.** Versioned releases on PyPI, `CHANGELOG.md`, `CITATION.cff`, CI on every PR. A release is a
   promise that the loaders in it work.
2. **Datasets and pipelines as small PRs.** A loader plus a catalog entry plus a smoke-test record is one PR.
   A pipeline is one YAML file with a citation.
3. **Results live in the repo and are regenerated by scripts.** Nothing hand-edited. The leaderboard and the smoke
   table are build artefacts.
4. **A dataset summary that is computed, not written.** Our `docs/decodable_datasets.md` already is.
5. **A one-call benchmark** so a new pipeline can be scored on the reference datasets in one line.
6. **"Bring your own BIDS."** `BIDSiEEGDataset(root=...)` already plays the role of `LocalBIDSDataset`.

## What we do differently, and why

- **Patient-specific evaluation.** EEG benchmarks pool subjects; ECoG grids differ per patient, so MOECoG never
  stacks subjects and reports per-patient distributions (`WithinSubjectCV`, chronological folds, kappa headline).
- **Regression is first-class.** Finger flexion, cursor, force and audio spectrogram targets sit next to trial
  classification, because that is where our decoders (PACE, TRACE) live.
- **A catalog with a status, not a wish list.** Every dataset entry says whether it loads today and why not
  otherwise; blocked and unsupported are statuses, not silence.
- **Smaller surface.** MOABB has 23 pipelines and dozens of datasets after nine years. MOECoG starts with the Miller
  library, the BCI competitions and a handful of loaders, and grows one chunk at a time (see `ROADMAP.md`).
- **No CodeCarbon, optuna or pseudo-online machinery yet.** They can come when someone needs them.

## What "hosted on the cloud" meant for MOABB

Nothing exotic: source on GitHub, package on PyPI, docs on GitHub Pages under a NeuroTechX domain, a Zenodo DOI
minted from GitHub releases, and result tables committed as CSV/JSON in the repository. Data are never hosted by
MOABB; loaders download from the original repositories into `~/mne_data`. MOECoG follows the same split: code
and numbers in the repo, data at the source, large caches on Athene or a personal disk (`MOECOG_DATA_DIR`).
